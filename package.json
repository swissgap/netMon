{
  "name": "gaming-day-network-monitor",
  "version": "1.0.0",
  "description": "Epic Gaming Day Network Monitoring Dashboard with automated device discovery",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "scan": "python3 network_scanner.py",
    "monitor": "npm run scan && npm start",
    "build": "echo 'No build step required - static HTML'",
    "install-python-deps": "pip3 install --break-system-packages python-nmap scapy pysnmp requests || pip3 install python-nmap scapy pysnmp requests"
  },
  "keywords": [
    "network-monitoring",
    "gaming",
    "dashboard",
    "snmp",
    "unifi"
  ],
  "author": "Gaming Day Team",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.14.2",
    "chokidar": "^3.5.3",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
